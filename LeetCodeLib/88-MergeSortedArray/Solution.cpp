#include "Solutions.h"

namespace MergeSortedArray
{
void Solution::merge(std::vector<int> &nums1, int m, std::vector<int> &nums2, int n) const
{
	[[maybe_unused]] auto _ = m + n + nums1.size() + nums2.size();
}
} // namespace MergeSortedArray
